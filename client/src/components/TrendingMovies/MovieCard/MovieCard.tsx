import React from 'react'
import { MovieCardContainer } from './MovieCard.elements'

const MovieCard = () => {
	return (
		<MovieCardContainer>
			<p>title</p>
			<p>date</p>
		</MovieCardContainer>
	)
}

export default MovieCard
